# Loop Video Maker — dependencias pinadas para Python 3.9
# Probado en macOS arm64 con Python 3.9.6

# UI
gradio==4.36.0
gradio_client==1.0.1      # requerido por gradio 4.36 (NO subir a 1.x mayor)
huggingface_hub==0.36.2   # NO usar >=1.0 (rompe gradio 4.36)

# Video
moviepy==2.2.1
pillow==10.4.0

# Audio
soundfile==0.13.1
numpy==2.0.2

# API clients
anthropic==0.88.0
requests==2.32.5
python-dotenv==1.2.1

# Opcionales (descomentar si se necesitan)
# google-generativeai>=0.7.0    # para generación de imágenes con Gemini
# torch>=2.0.0                  # para MusicGen (requiere ~5GB de RAM)
# transformers>=4.40.0          # para MusicGen
